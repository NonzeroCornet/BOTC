{
  "name": "botc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node index.js",
    "build": "terser public/client.js -o public/client.min.js --compress --mangle && cleancss public/styles.css -o public/styles.min.css && cleancss public/create.css -o public/create.min.css",
    "docker-build": "npm run build && docker build -t botc-app . && docker save botc-app -o botc-app.tar"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.46.0"
  }
}
